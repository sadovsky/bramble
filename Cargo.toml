[workspace]
resolver = "2"
members = ["abi", "graph", "kernel"]
exclude = ["user"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.release]
debug = true
overflow-checks = true
